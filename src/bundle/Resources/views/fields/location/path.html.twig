{% for location in locations %}
    <a href="{{ path('_ez_content_view', {'locationId': location.id}) }}">{{ ez_content_name(location.contentInfo) }}</a>
    {% if not loop.last %}
    / <br>
    {% endif %}
{% endfor %}
